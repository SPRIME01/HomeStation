# Minimal, schema-safe values for community-charts/n8n.
# Customize by first inspecting chart defaults:
#   helm show values community-charts/n8n > /tmp/n8n.values
# and only adding keys supported by that chart version.

service:
  type: ClusterIP

