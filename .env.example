# Non-secret local defaults for this project (copy to .env)
# This file is safe to commit. Do NOT put secrets here.

# Cluster/domain
DOMAIN=homelab.lan

# Helm/Just toggles
PROMTAIL_ENABLED=0
HELM_NO_CREDS=0
N8N_USE_COMMUNITY_VALUES=1

# Public TLS (optional)
#TLS_PUBLIC_DOMAIN=example.com
#ACME_EMAIL=you@example.com
