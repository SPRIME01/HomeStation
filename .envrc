# Load Vault session exports if present
# This file is created by `just vault-init` when you choose to save the token.
source_env_if_exists tools/secrets/.envrc.vault
# Legacy fallback (pre-change):
source_env_if_exists tools/secrets/.env.vault

# Optional: Cloudflare DNS token for cert-manager (ACME DNS-01)
# Put `export CF_API_TOKEN=...` in tools/secrets/.envrc.cloudflare
source_env_if_exists tools/secrets/.envrc.cloudflare

# You can override defaults used by the Justfile here, e.g.:
# export DOMAIN=homelab.lan
